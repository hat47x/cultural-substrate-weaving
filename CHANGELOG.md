# Changelog

## Unreleased

- Added `10-integration`: raising a whole picture out of many fragments (carding, grouping, labelling, placement, narration) with blank detection as its primary product.
- `02-system-selection`: distinguish a framework's position layer from its interpretive layer, decided by predicate type (counting / computing / similarity). Only the position layer classifies; the interpretive layer generates.
- `01-scope-and-facts`: write the baseline out as a finished product rather than self-reporting it. Increment cannot be measured by coverage of the exploration axes.
- `00-iteration`: change the question each pass; blanks instruct the next round of gathering; try an untried stage before stopping.
- `02-system-selection`: added the removal check as a fourth post-adoption check — delete the framework's names and tables and judge finding by finding what still stands. It asks whether a finding survives, not whether the question was re-derivable without the framework, so the position layer's output is not discarded. Adopted frameworks now land in one of four states, and the signs of over-application are named.
- `01-scope-and-facts`: list what must not be broken alongside the baseline. A change the writer makes to the problem statement is not a finding of this method.
- `00-iteration`: do not file a pass's output back into the existing structure; hand off blanks that have become domain implementation questions instead of filling them.
- `10-integration`: a question from an external framework is not observed fact until returned to the target. Zero cross-check difference shows traceability, not completeness.
- Local inspection of the accumulated edits, no new rules: the assignment rule and the external convention it introduces now precede the clauses that refer to them (they had drifted below through successive insertions); the substitution check no longer sets a contribution figure that the removal check then contradicts; the five likely additions moved into the operation that runs at every grain, from the audit that runs once at the end; the operation is listed as running at every fold, not only at carding and labelling; and the router's stop test carries the conclusion-movement criterion the detail file gained.
- `00-iteration`: tell whether a pass froze by whether the conclusion moved, not by whether the text grew. Measured: over six passes on one target, asking for a single conclusion each time and showing only the history of conclusions, both control runs reinforced early — one froze on pass 1→2, the other through 2→3 — while both skill runs replaced the central claim at every one of five transitions. Three blind judges, unaware two conditions existed, agreed 95–100% on whether a transition moved and put the two arms at 5/5 against 4/5 and 3/5. Single-pass comparison had shown nothing; so had counting findings per pass. **Corrected on re-measurement:** re-judged blind against six series including a two-clause arm, the same histories came out 5/5 and 4/5 for the skill arm against 4/5 and 3/5 twice, and the two-clause arm landed on exactly the numbers the control arm had, so those two clauses are not what produced the separation. One series moved by a point between judgings on identical text, so the gap is about one transition wide and sits inside how sensitive the instrument is to what it is compared against. The diagnostic stands; the effect size is not established.
- `ROUTER`: the minimum procedure named only one route to an increment. It now names both — take structure from a framework, or integrate the target's own material and find its blanks — with the reading order pointing at `10-integration` when material is plentiful or the structure unsettled.
- `06-output-and-collaboration`: put the falsification condition into the artifact, not only the working record.
- `10-integration`, `01-scope-and-facts`, `03-transformation`, `02-system-selection`: five clauses that loosen rather than restrain, from two handover documents. A card carries four fields and no more — source, path, stage, and who collected it when — with no field for confidence, role, importance, feeling, or type; those belong in the sentence, since a type assigned up front is what every later look goes through. A field whose value repeats may be held on the set and written back when cards of different origin meet. Of these, one item per card and a readable sentence are attested KJ practice; source, path and collector are not attested in the public sources and are carried as modern additions. An attention you cannot give a reason for goes onto a card without one; how strongly something catches you and how likely your reading is to be right are separate. Separate observation, recurrence, function, and intent — function may be named, intent is not to be read. Do not set the threshold for collecting at the threshold for publishing. And what a framework produced is kept with its provenance rather than held back.
- `00-principles-and-constraints`: the analyst's own values and default stances are structures brought in from outside the target, and get the same checks as a cultural framework; where a constraint means weakening or cutting the account, record what was lost and count that loss the way an addition is counted. Also: do not widen an entrusted scope, and do not narrow it through excessive self-restraint.
- `01-scope-and-facts`: do not infer sameness or centrality from how often something recurs; establish what each occurrence does first.
- `09-evaluation-and-domains`: do not balance an account into "problems on both sides" where an asymmetry of power, responsibility, or harm actually exists.
- `10-integration`: record the differences dropped on purpose, and add change-of-confidence to what a return check looks for.
- `10-integration`: covering the material cuts lifted wording but not distance from it — the space it opens fills with the analyst's vocabulary, and the actor drops out, turning what someone chose into something that occurred. Measured, so the clause now carries its own cost. Also: settle whether a label or its grouping is at fault by raising the label twice; matching stumbles indict the grouping.
- `10-integration`: carding, labelling, and folding labels together are stated once as a single operation called at different grains, rather than as separate steps with separate rules. Its boundary rule runs in both directions — join to protect a meaning, split to protect the state of the evidence — and its label is raised from extracted cores with the material covered, then checked back against it. Also: keep the source separate from the path it was found by; do not group by source or speaker; do not count a repost or a derived card as an independent appeal; and name the four things a return check finds added.
- `08-governance-and-records`, `09-evaluation-and-domains`: record judgment origin, preserved items, and adoption state; do not count one change several times.
- `02-system-selection`: split the position layer into a derivable part and a conventional part. The conventional part carries the lineage, so declare which lineage, cite it, and name an alternative; a lineage declared from recall alone is a self-report rather than a checkable operation. Check the derivable part mechanically before use.
- `08-governance-and-records`: keep framework content out of the static layer. The static layer holds the procedure for retrieving, checking, and returning it — not the tables.
- `02-system-selection`: a misfit you could have written before looking at the target is not a finding about it — list the framework's structural blind spots before applying it, and subtract them afterward. And record what share of a convention's vocabulary ever matched: two conventions can agree because both missed the same way.
- `02-system-selection`: a determinate assignment is not a correct one — a mechanical predicate reproduces its errors as faithfully as its results, which is what makes them correctable. Take what filled a position back to the target. And separate the two kinds of empty position: no candidate present, versus candidates present but the relation never named.
- `02-system-selection`: counting something does not make it a counting predicate. Only mechanical membership qualifies; counting a set the analyst rules on item by item is a similarity judgment whose numeric output hides the judgment. Report the margin that decided the value, and declare whether the predicate applies to the events narrated or to the act of narrating.
- `02-system-selection`: a framework supplies the number of positions but not the rule for cutting the target into that many; the cut is part of the assignment. Settle whether the external convention is really in place by assigning twice independently, and do not build a conclusion on a position that came out split. Self-reported uncertainty does not predict which positions split.
- `02-system-selection`: the predicate typology names three ways an assignment gets decided — counting, computing, similarity. Both runs independently found a fourth thing that is none of them and can dominate the result: stipulations inside the convention itself, coming from neither the framework nor the target. One run named them, separated them in its convention text, and measured that a single such choice — take the last qualifying sentence rather than the first — moved a boundary by 59 sentences. The other run found that the region its analysis left dark was exactly the region that choice had excluded. Name them and measure how far the result moves when they are set the other way.
- `02-system-selection`: two of the file's own rules were in tension and a run found the join. The removal check counts as a result only what stands once the framework's vocabulary is deleted; the four adoption states offer "carried into the artifact" as one destination. Nothing can reach that destination except where the target itself carries the framework or the artifact is about method — so the run placed nothing there and warned that an empty first state is the shape of where two rules meet, not evidence of a strict check. The four states also folded a framework's validity together with how much it produced, leaving no way to place a framework that was rejected and still yielded findings — which is what happened: the rejected first-choice framework produced three surviving findings and a framework brought in only as a control produced five, more than any other. Rejection is now stated not to retract findings, the state is decided by validity, auxiliary model is glossed the way internal scaffolding already was, and a finding that came out of two frameworks is recorded under both without being counted twice.
- `02-system-selection`: counting the signs of over-application had the same one-polarity defect as the clauses above — a count of zero reads as a clean result. A run measured what its own account actually covered (50 of 110 sentences, with three contiguous stretches left dark) and found that the conflicts its zero count seemed to vindicate it for not absorbing were simply never reached. Measure the coverage before reading the count.
- `02-system-selection`: the substitution check covered only alternatives that were applied. A run reported two frameworks it declined to apply — one whose segmentation the framework itself does not supply, one with no external assignment procedure at all — and noted that it was recording them apart from frameworks it applied without result, so as not to read "could not run it" as "the absence did not reproduce." Those are opposite conclusions and the clause did not separate them.
- Measured, no change: the claim that the places a framework missed are more useful than the places it hit had never been counted. One run counted it — of 21 findings that survived the removal check, at least 8 came from re-reading the target to resolve a misfit. The same run predicted which of its own surviving misfits would fail the removal check (the five that were statements about the convention rather than the target) and was right about all five, so the misfit record and the removal check compose as intended.
- `02-system-selection`: unit compatibility was written as a single yes/no between a framework and a target. Two independent runs of the file over one target found the same defect from different frameworks: one reported that compatibility "does not come out single — it is decided at three junctions, incompatible at two and compatible at one"; the other stated it as a rule, that compatibility belongs to the framework paired with a layer, and used it to invalidate the empty positions its own three preceding steps had produced. The second run also supplied the operative test: units of the same kind still make an incompatible pair when the principle of division differs — the framework divided its positions by order while the target divided itself by whether a passage was necessary to the talk, and said so in its own words.
- `02-system-selection`: a third kind of empty position — empty in the layer the predicate was declared for, full in another. Both runs hit it on the same position: the ruler/subject vocabulary appears only inside the story the target quotes, never in the frame that quotes it, so the position reverses between full and empty when the layer changes. It is neither a property of the target nor a gap in the description, and reporting it as "empty" claims something about the target that was measured outside the declared layer.
- `02-system-selection`: the mechanical check applied to the derivable part had been written as a bar on the whole position layer — "do not use a structure you cannot check" — which, read as written, disqualifies a framework that has no derivable part at all. Found in execution: running the file end to end against the five Confucian relations, the run reported that the framework's position set cannot be machine-checked and so is structurally weak as a position layer, having just used it correctly under a counting predicate. The bar now falls on a derivable part that fails its check, and where there is no derivable part, what gets checked is the external convention instead.
- `02-system-selection`: reporting the margin that decided a count was written as if only a small margin were suspect, which reads as reassurance whenever the margin is wide. Measured across one full run of the file: the clause fired eleven times, and three of those firings said the opposite — a 39-sentence margin was wide only because the candidates were sparse; a position's 5-to-0 margin rested entirely on one line drawn one way; and token counts of 5:1, 4:1 and 3:1 collapsed to one relation each, so the margin was measuring occurrences of a word rather than instances of a relation. The clause now says what it is for: making visible whether the target or the convention decided the value.
- `02-system-selection`: recording how much of a convention's vocabulary ever matched guarded only the failure where that share comes out low. The same run measured the opposite failure: 95.7% of the vocabulary matched at least once while only 46.4% of the target landed in any position at all. Two coverages are now counted rather than one, because a high vocabulary share can sit on top of a framework that touched a fragment of the target.
- `01-scope-and-facts`: the requirement to state a comparison named only "sparse" and "missing." `02-system-selection` sends the analyst after absence *and skew*, so half the judgments the method routinely makes were passing an unguarded gate. Found by sweeping for the shape the two corrections above share — a detection signal written in one polarity — rather than measured; recorded as such.
- The three entries below come from one sweep of every file for rules that reduce what gets considered, sorted by the distinction `01-scope-and-facts` already draws between the threshold for collecting and the threshold for publishing. Rules that fire at the publishing gate were left alone; the three that fired at the collecting gate are corrected. This is the check `08-governance-and-records` asks for — the ratio of suppressive to generative rules — run for the first time. Swept, not measured; recorded as such.
- `00-activation`, `ROUTER`: the tie-break sent every evenly balanced activation decision to non-activation, and balanced is exactly the state in which the increment is unknown. `00-iteration` already carries the reason that cannot be read as a result — the return on an untried move cannot be measured, so a verdict of "no increment" is self-fulfilling — but the activation gate was decided before any clause of the method could apply. A balanced decision now runs at the cheapest scope and measures. Precision-first survives for implicit activation, restated as what it is for: not interrupting unrelated work.
- `00-activation`: explicit invocation could be declined on a prediction. It was the one place the method overrode the requester's own decision to invoke it, and it did so on the weakest evidence available — a forecast made before the baseline was written. The five required constraints already forbid narrowing an entrusted scope through excessive self-restraint, and an explicit invocation is such a scope. Explicit invocation now replaces the activation decision: what gets adjusted is the scope, not whether to activate. Where ordinary methods look sufficient, the limited application still runs, so "no increment" is a measurement rather than a forecast. A scope narrower than the one specified must be declared with a reason measured on the target.
- `10-integration`: the group counts — two or three at the first level, five to seven folded, at most ten — read as caps, so material needing more had nowhere to go and the procedure's convenience outranked the material's shape. They are stated as guides now, and a count outside them raises the question of whether the material requires it or the grouping failed, which is what the failure-sign list already did for the first level. Splitting or merging groups to reach a number is named as the thing not to do.
- `10-integration`: the file carried the KJ method's conventional parts — the card and group counts, the ban on noun endings, a label that is not a category name — as if they were its own rules, with the lineage stated nowhere a reader of the skill would find it (KJ appeared once in this changelog and in no shipped file). `02-system-selection` requires exactly this of any adopted framework: declare the lineage, cite the source, name at least one alternative and its difference. The method had not run its own check on the framework it imported. The lineage is now declared against Kawakita's 1986 summation with the 1967 and 1970 paperbacks as supporting sources, the trademark is noted, and the affinity diagram is named as the alternative — the 1977 QC adaptation of the A-type diagram, renamed to avoid the trademark, which leans from ideation toward organization. `08-governance-and-records` also forbids freezing framework content in the static layer, so an unsettled judgment is now sent to the primary sources, with the unreachable case written out: record it as self-reported and proceed. Which parts are attested practice and which are modern additions is stated in the file rather than only here.
- `02-system-selection`, one case, no change: the lineage clause warns that memory drifts toward the most-published version, "often the newest overpainting." For the KJ method the direction reverses — the most-published are the 1967 and 1970 paperbacks, the earliest and most simplified, while the least-read 1986 volume is the author's most complete statement. The drift is real and the clause caught it; the direction it names is one case wide and did not hold here. `10-integration` carries the correction locally rather than restating the clause.
- `10-integration`: restraint was landing on the card. Where material is charged, the pull is to even each card as it is written — soften an accusation into an observation, attach a qualifier, turn "X chose this" into "this occurred" — and the file already named that drift, that rewriting a choice into an occurrence loses the self-blame the material carried and leans toward exoneration, without naming the analyst's own ethics among its drivers. Three things break. The card stops being a record. The poles the diagram was to draw opposition between are gone at input, so the existing failure sign "no opposition appears in the diagram" fires with its cause on the card side, where the diagram cannot show it. And the suppression leaves no trace, which is the collecting-versus-publishing threshold violation `01-scope-and-facts` already forbids for every other driver. Opposing views are now stated to be raised as separate cards rather than balanced within one — one item to a card makes this so — the point of application is named (placement, narration, output, not the card), and `00-principles-and-constraints`'s rule that the analyst's own stance takes the framework's checks is connected to the place the card operation happens, which is the connection repair `08-governance-and-records` prefers over deletion. A weakened card must carry what was changed and what was lost. Three failure signs added. Reported by a user of the method; not measured.
- Build: the token budget measured six delivery files and none of the thirteen reference files, which are ninety per cent of the source. Ninety per cent of the corpus therefore had no ceiling and no failure condition, and the two largest files had grown past the size guideline for a SKILL.md without anything registering. `token_budget.py` now measures every source file and reports a per-locale corpus total; `validate.py` enforces a per-reference ceiling and a corpus ceiling. Both are ratchets: lower them when reduction lands, never raise them for growth.
- Removed the per-file preamble "select from the lists according to the target; put target-specific additions in the dynamic layer" from all ten reference files. `ROUTER` already carries both halves, and `ROUTER` is always in context before any reference loads.
- `02-system-selection`, `10-integration`: cut restatement and second explanations where the rule and its check both survive without them — what the position layer buys, why an empty first state is expected, why a vivid fit is tempting to write up, why re-derivability would discard the position layer's output. The guard sentence was kept in every case; only the sentence that explained an adjacent sentence was removed.
- `02-system-selection`: the lineage clause said recall leans toward the most-published version, "often the newest overpainting." Corrected to name both directions, the KJ case having come out the other way.
- Measured, and three estimates were wrong: a sweep for peripheral content predicted 10–15% from duplication, 8–10% from stripping rationale, and 25–35% overall. Exact cross-file duplicate detection found 371 redundant characters in Japanese and 881 in English — 1.2–1.4%, all of it the one preamble. The 10–15% figure came from grepping keywords and reading co-occurrence as duplication; `01-scope-and-facts` already forbids that inference, that recurrence does not establish sameness and each occurrence has to be checked for what it does. Stripping rationale yielded about 1%, because most sentences that look like explanation name something to check. Achieved: ja-JP 74189 to 72292 bytes, en-US 74259 to 72675 — 2.6% and 2.1%. Recorded because the estimates were stated before they were measured, three times.
- Reviewed and left unchanged, with reasons: `07-creative-patterns` and `05-character-and-taiheki` were candidates for removal on corpus size, but reference material loads on demand and costs nothing until it does, so removing them would trade capability for no saving in any run that does not load them. `09-evaluation-and-domains` §11 was a deletion candidate as pure restatement — every one of its thirteen items restates a rule from another file — but a run of the method against a sample target with §11 withheld, then applied, had six of the thirteen fire on work actually skipped, including a proposal that changed a stated preserved item without the preservation check being run. Its function is detecting omission, not supplying rules, and that function is not duplicated. One run, by an analyst who knew the checklist existed, which biases toward fewer firings.
- Split `02-system-selection` into `02-system-selection` (candidates, uses, unit compatibility, rejection conditions, precedents) and `02a-framework-application` (the assignment procedure and the four post-adoption checks). At 106 lines the single file was over the length at which a reference is supposed to carry a table of contents; both halves are now near 55. Measured per-run: a run that surveys a framework and rejects it now loads 9.4KB where it loaded 20.5KB, a 54% cut, since rejection completes in the first file. A run that adopts loads 3.6% more. The split creates a path where the assignment could be run without the checks that decide what counts as a result, so the first file ends by requiring the second by name and stating that a finding that has not been through those checks is not a result, and the router says the same.
- The corpus ratchet added one step earlier caught the split immediately: total bytes rose while the change was still uncommitted. The ceiling was raised, which the ratchet's own rule forbids for growth, so the rule now names the one exception — a reviewed structural change, recorded in the budget file with what it bought. Here it bought the largest single reference falling from 14509 to 11856 bytes, and that ceiling ratcheted down with it. Total corpus: ja-JP 74189 to 72962, en-US 74259 to 73377.
- Build: a cross-reference written as a markdown link inside a reference file would have shipped broken, since the build rewrites router links but copies reference files verbatim. Cross-references between reference files are written as bare skill-reference filenames, which resolve because the built references sit in one directory.
- Codex moved from standalone skills to plugins: `openai/skills` was deprecated on 2026-06-22 in favour of `openai/plugins`, and on 2026-07-09 the app directory folded into the plugin directory, making plugins the discovery path across both ChatGPT and Codex. The build now emits a `.codex-plugin/plugin.json` into the same plugin directory Claude Code already uses, plus a Codex marketplace at `.agents/plugins/marketplace.json`. Both ecosystems read skills from `skills/<name>/SKILL.md`, so one plugin directory carries two manifests rather than duplicating the reference tree. `codex plugin marketplace add hat47x/cultural-substrate-weaving` now installs without a release download, the same way the Claude Code path already worked. Manifest and marketplace shapes were taken from a real plugin in `openai/plugins`, not from the prose docs. The older skill layout is still built and still documented, for users who already installed it.
- `validate.py` checks the Codex marketplace the way it checks the Claude one: plugin set, manifest presence, version match, and that a declared skills directory actually contains a `SKILL.md`.
- README (both locales): the install commands were reachable only by following a link into the platform guides. They are now the first section, one runnable line each for Claude Code and Codex, with the release download named as the path for ChatGPT and Microsoft 365 only. The delivery route for Claude Code had been complete since 0.2.0 — marketplace manifest and built plugins are committed, so the repository installs directly — but nothing on the front page said so.
- README (both locales): added the hypothesis about why an external framework helps at all, since the method is about to be offered to people who have not read the changelog. The claim is not that a cultural framework is true; it is that one acts the way a pretrained representation acts in transfer learning — a prior carrying structure that a single target cannot induce, whose value is decided by performance on the target rather than by its own correctness. The correspondence is stated term by term, including negative transfer against over-application and ablation against the removal check, and so is the falsification condition: no transfer occurred if findings surviving the removal check do not exceed the baseline.

- Renamed the plugins from `csw-method-{ja,en}` to `cultural-substrate-weaving-{ja,en}`, and the skill to `weave`, so invocation reads `/cultural-substrate-weaving-ja:weave` rather than an initialism nobody can expand. Done before the community-marketplace submission, because a catalogue pins by name and the rename gets expensive afterwards.
- `validate.py`: added `check_reference_links`, which resolves every markdown link inside an installed reference file. This came out of applying the method to the validation suite itself, recorded as case 1 in the worked examples. Assigning each check to the artifact layer it reads left the position past the last one empty: nothing reads the artifact as the consumer receives it. The gap was real — earlier in the same session a link written against the source layout passed `make check` and would have shipped dead, caught by reading the build rather than by any check. The new check was verified by re-injecting that bug.
- Added `docs/{ja,en}/examples.md`: applied cases written as measurement records, each with its baseline, the framework pass, all four post-adoption checks, and an honest tally. Two cases, twelve baseline findings, three surviving the removal check, and zero confirmed specific to a framework — the substitution check says a plain pipeline model would have found case 1 too. Recorded rather than hidden.
- Both READMEs and both plugin READMEs now state that the method is under validation, since two cases do not support a stronger claim.

- Audited the text against what the method is actually for, and it did not carry it. Five gaps, all corrected. **Activation had no test for whether the problem space is open**, so a closed one satisfying structure, interacting dimensions, verifiability and plausible increment passed — which is exactly how case 1 of the worked examples came to be a misapplication the text authorized. Openness is now the first of five conditions, and a closed problem space fails on it while satisfying the rest. **Stopping was decided partly by cost**, which fires precisely while paying for the breakthrough the method exists to reach; stopping is now decided by exhaustion of material — no new card rises, the increment duplicates what is there, the framework returns what the baseline already had — and cost is named as the human's call, not the method's. **The framework could only be used statically.** A fourth use, the transition model, treats the framework as a state machine run over the target, with a procedure in `02a-framework-application` that lays the states as a grid and sorts the run into states never reached, transitions that never fire, and states entered but never left. **Contribution was defined as a count**, which invited exactly the scoreboard the first two examples were written as; a count is now stated to be a guard against overclaiming, used in one direction only. And `ROUTER` now says what the method is for: getting past where repeating the same move stops paying, at the cost of more passes, not the same result from fewer tokens.
- `docs/{ja,en}/examples.md`: case 1 is relabelled a record of misapplication rather than a demonstration, and kept, because it is what produced the openness condition. The header now warns against reading the tallies as a score.
- The corpus tripwire fired a second time, on plain content growth this time, which the rule as written forbade outright. Both firings did the same thing — stop, name the change, record what it bought — and neither prevented anything, so the rule is restated as what it demonstrably is: a tripwire that forces accounting, not a cap. Raises now require a line in `_history` saying what was bought. This growth bought the method's stated purpose: ja-JP 72962 to 78048, en-US 73377 to 78109.

## 0.1.0 — Initial public release

- Semantic canonical source under `src/ja-JP/`, with a structurally parallel English translation under `src/en-US/`.
- Glossary, translation source hashes, and review policy under `i18n/`.
- Locale-aware builders for Codex, Claude Code, custom GPTs, and Microsoft 365 Copilot.
- Language-specific release assets and beginner guides.
- CI checks for source-tree parity and stale translations.
