name: Validate

on:
  push:
    branches:
      - main
      - "develop/**"
      - "release/**"
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Verify branch version contract
        shell: bash
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          ref="${GITHUB_REF_NAME}"
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
            ref="${HEAD_REF}"
          fi
          python scripts/check_branch_version.py --ref "${ref}" --version-file VERSION
      - name: Run validation
        shell: bash
        run: |
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]] || \
             [[ "${GITHUB_REF_NAME}" == "main" ]] || \
             [[ "${GITHUB_REF_NAME}" == release/* ]]; then
            make release-check
          else
            make check
          fi
      - name: Verify tracked generated artifacts are current
        run: git diff --exit-code -- .claude-plugin .agents plugins
      - uses: actions/upload-artifact@v7
        with:
          name: multilingual-validation-reports
          path: dist/reports/
      - uses: actions/upload-artifact@v7
        with:
          name: release-candidate-packages
          path: dist/packages/
          if-no-files-found: ignore
