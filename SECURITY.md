# Security policy

This repository contains instructions, documentation, and packaging scripts. It does not require runtime secrets.

Do not commit Microsoft tenant credentials, API keys, `.env.*.user` files, private SharePoint URLs, or unpublished source documents.

Report a security issue through GitHub private vulnerability reporting when available. Otherwise, open an issue that contains no sensitive details and ask the maintainer for a private contact channel.
