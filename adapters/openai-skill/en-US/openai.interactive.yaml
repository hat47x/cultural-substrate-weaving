interface:
  display_name: "Cultural Substrate Weaving — English"
  short_description: "Explore structure candidates, relations, and gaps with cultural frameworks and KJ, then validate them on the target"
  default_prompt: "Establish the domain-method baseline and validate only the increment produced by cultural frameworks and KJ integration."
policy:
  allow_implicit_invocation: true
