interface:
  display_name: "Cultural Substrate Weaving — English"
  short_description: "Validate structures on the target and transform them into target-specific judgments and designs"
  default_prompt: "Establish the ordinary-method baseline and apply this method only when a meaningful increment is plausible."
policy:
  allow_implicit_invocation: false
