# Use from AGENTS.md or CLAUDE.md

These files may be loaded on every task, so do not place the full method in them.

Include only:

1. the kinds of tasks that may require the method;
2. the installed skill or document entry point;
3. the kinds of routine tasks where it should not be used.

Localized fragments are under `adapters/project-integrations/<locale>/`.
